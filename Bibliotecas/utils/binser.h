#ifndef BINSER_H
#define BINSER_H

// Funções para serialização binária (placeholder)
int binser_serialize(const void* data, int size, void* out, int out_size);
int binser_deserialize(const void* in, int in_size, void* out, int out_size);

#endif // BINSER_H
