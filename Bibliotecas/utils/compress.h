#ifndef COMPRESS_H
#define COMPRESS_H

// Funções para compressão (placeholder)
int compress_data(const void* in, int in_size, void* out, int out_size);
int decompress_data(const void* in, int in_size, void* out, int out_size);

#endif // COMPRESS_H
